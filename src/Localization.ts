export interface ILocalization {
    cancel: string;
    remove: string;
    add: string;
    submit: string;
}

export const Localization: ILocalization = {
    cancel: 'Cancel',
    remove: 'Remove',
    add: 'Add',
    submit: 'Submit'
}
